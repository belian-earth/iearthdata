% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/query.R
\name{query_files}
\alias{query_files}
\title{Query files in a dataset intersecting a bounding box or polygon}
\usage{
query_files(id, geometry, time = NULL)
}
\arguments{
\item{id}{Dataset id from \code{\link[=list_datasets]{list_datasets()}}.}

\item{geometry}{A numeric bbox \code{c(xmin, ymin, xmax, ymax)} in WGS84,
an \code{sf}/\code{sfc}/\code{bbox} object, a length-1 WKT POLYGON string, or any
\code{wk}-handleable geometry. Finer geometries are coerced to their
bounding box server-side; do local filtering on the returned tiles
if a stricter intersection is needed.}

\item{time}{Optional vector of path-segment values to filter the
catalog (typically years, e.g. \code{2018:2020}). When \code{NULL}, all
leaf paths are queried.}
}
\value{
A tibble with columns \code{gid}, \code{file}, \code{size}, \code{path},
\code{geometry} (wk_wkt), \code{dataset_id}, \code{r_id}, \code{suffix}, \code{table}.
}
\description{
Resolves the dataset's catalog tree, expands it to leaf-level paths
(optionally filtered by \code{time}), and issues a \code{polygonQuery} against
each leaf path. Results are combined into a single tibble with a
\code{wk_wkt} geometry column carrying each tile's bounding box.
}
