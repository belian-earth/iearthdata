% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{get_signed_url}
\alias{get_signed_url}
\title{Get pre-signed download URLs for files}
\usage{
get_signed_url(files, dataset_id = NULL, max_active = 6L)
}
\arguments{
\item{files}{A tibble from \code{\link[=query_files]{query_files()}} / \code{\link[=esd_query]{esd_query()}}, or a
character vector of fully-formed object keys (in which case
\code{dataset_id} must be supplied so the resourceId can be looked up).}

\item{dataset_id}{Required when \code{files} is a character vector.}

\item{max_active}{Maximum concurrent requests.}
}
\value{
A character vector of pre-signed URLs aligned with \code{files}.
Failed requests yield \code{NA} with a \code{warning} carrying the HTTP
status and (where available) the response body.
}
\description{
Issues \code{POST /starcloud/api/file/downloadResource} for each file and
returns the pre-signed URLs. Requests are fanned out in parallel via
\code{\link[httr2:req_perform_parallel]{httr2::req_perform_parallel()}}.
}
\details{
The \code{objectKey} sent for each file follows the format the iEarth UI
constructs in chunk 813 of the SPA:
\verb{shared-dataset/<dataset_type>/<catalog_path>/<filename>}, with
spaces in the dataset type replaced with underscores. For example,
an ESD tile is keyed as
\verb{shared-dataset/SDC30_EBD/SDC30_EBD_V001/2020/<file>}. The literal
\code{"shared-dataset"} prefix is hardcoded in the SPA call site
(\code{rootPath: "shared-dataset"}); the dataset's \code{resourceId} is sent
separately in the body.
}
