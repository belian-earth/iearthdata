% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download.R
\name{as_vsicurl}
\alias{as_vsicurl}
\title{Wrap a signed URL in GDAL's \verb{/vsicurl?use_head=no&url=...} form}
\usage{
as_vsicurl(url)
}
\arguments{
\item{url}{Character vector of signed URLs. \code{NA} and empty strings
pass through unchanged.}
}
\value{
A character vector of \verb{/vsicurl?...} paths.
}
\description{
OBS pre-signed URLs are method-specific (signed for \code{GET}), so GDAL's
default \code{HEAD} probe against \verb{/vsicurl/<url>} fails with a 403. The
query-string variant introduced in GDAL 3.6 lets us disable the
probe per-URL without touching the global \code{CPL_VSIL_CURL_USE_HEAD}
env var.
}
